package:
  name: elemental-backend
  version: "0.2.0dev1"

source:
  git_url: https://github.com/artplusplus/elemental-backend.git
#  git_rev: v0.2.0-dev1
  git_tag: travis_ci_conda_support

build:
  number: 0

requirements:
  build:
    - python
    - setuptools
  run:
    - python

test:
  requires:
    - pytest
  imports:
    - elemental_backend

about:
  home: https://github.com/artplusplus/elemental-backend.
  summary: elemental-backend provides a data backend for elemental cms.
  license: Mozilla Public License 2.0 (MPL 2.0)
  license_file: LICENSE